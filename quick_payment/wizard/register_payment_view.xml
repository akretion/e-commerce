<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

        <record model="ir.ui.view" id="register_payment_view">
            <field name="model">register.payment</field>
            <field name="arch" type="xml">
                <form string="Register Payment" version="7.0">
                    <group>
                        <field name="description" string="Description"/>
                        <field name="journal_id" string="Journal"/>
                        <field name="amount" string="Paid Amount"/>
                        <field name="date" string="Date"/>
                    </group>
                    <footer>
                        <button string="Pay" name="register_payment" type="object" class="oe_highlight"/>
                        or
                        <button string="Pay And Confirm" name="register_payment_and_confirm" type="object" class="oe_highlight"/>
                        or
                        <button string="Cancel" class="oe_link" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="open_register_payment" model="ir.actions.act_window">
            <field name="name">Register Payment</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">register.payment</field>
            <field name="view_type">form</field>
            <field name="target">new</field>
            <field name="view_id" ref="register_payment_view"/>
        </record>

    </data>
</openerp>
